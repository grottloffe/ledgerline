# {{PROJECT_NAME}} — Lessons learned

Append-only. Newest at the bottom.

A lesson is only worth writing if it changes what we do next time. Each entry
ends in a **rule** — a sentence a future session can act on without reading the
story. If you cannot write the rule, you have an anecdote, not a lesson.

Sources worth mining: a bug that took far longer than expected, a plan whose
estimate was wrong by more than 2×, a review comment that recurred, a wrong
assumption about a library or API, a debugging session that went down a dead end.

Kind: `process` · `technical` · `estimation` · `tooling` · `product`

---

### L-0NN — (replace: the rule, compressed into a title)

- **Date:** {{DATE}}
- **Kind:** process
- **What happened:** (the concrete event, one or two sentences)
- **Why:** (the root cause, not the symptom)
- **Rule going forward:** (the actionable sentence)
- **Applied to:** (skill, CLAUDE.md, template, or checklist you changed — or "nothing yet")
- **Links:** —
